---

  - name: Install brew packages
    shell:
      cmd: "brew install {{ item }}"
    with_items:
      - bat
      - curl
      - docker
      - docker-compose
      - fd
      - fish
      - fzf
      - gcc
      - git
      - git-split-diffs
      - golang
      - golangci-lint
      - jq
      - kubectl
      - kubectx
      - llvm
      - node
      - npm
      - postgresql
      - protoc-gen-go-grpc
      - python3
      - ripgrep
      - rustup
      - shellcheck
      - starship
      - telnet
      - the_silver_searcher
      - tmux
      - translate-shell
      - tree
      - vim
      - wget
      - yarn
      - zsh
    ignore_errors: true
    tags:
      - brew
